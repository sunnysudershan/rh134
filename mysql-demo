 podman search mysql-57
 podman run -d registry.access.redhat.com/rhscl/mysql-57-rhel7
 podman logs gallant_mahavira
 podman run -d --name mysql-demo -p 13306:3306 -e MYSQL_USER=sunny -e MYSQL_PASSWORD=redhat -e MYSQL_DATABASE=apps -e MYSQL_ROOT_PASSWORD=redhat registry.access.redhat.com/rhscl/mysql-57-rhel7
 sudo dnf install mysql -y
 mysql -usunny -predhat  -h demo01.example.com -P 13306
 mysql> use apps
 CREATE TABLE movies(title VARCHAR(50) NOT NULL,genre VARCHAR(30) NOT NULL,director VARCHAR(60) NOT NULL,release_year INT NOT NULL,PRIMARY KEY(title));

  
